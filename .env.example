# ============================================================
# 可信 RAG 问答系统 — 环境变量示例
# 复制本文件为 .env 并填入真实值；.env 已加入 .gitignore，切勿提交。
# 注意：绝对不要在本文件或任何提交材料中写入真实密钥。
# ============================================================

# ---- 可选：大语言模型（不配置时系统使用内置规则/抽取式回答，仍可完整运行）----
# 仅支持从环境变量读取，禁止硬编码在代码中。
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=

# ---- 可选：本地 Embedding 模型（用于 Hybrid 检索，不配置时使用 BM25）----
# EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
